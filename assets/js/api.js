export async function getBundle(name, params={}){const qs=new URLSearchParams(params).toString();const r=await fetch(`/api/public/${name}?${qs}`);if(!r.ok) throw new Error('API error');return r.json();}