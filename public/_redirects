# Fix .html pages - redirect to non-html version
# Cloudflare Pages automatically serves .html files for requests without extension
# This file ensures backward compatibility

# Remove the problematic wildcard redirect that breaks .html pages
